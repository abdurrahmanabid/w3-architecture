import { Button } from "../ui/button";

const ButtonOrange = () => {
  return (
    <div>
      <Button className="bg-[#ff900e] font-bold mb-4 ">Explore more</Button>
    </div>
  );
};

export default ButtonOrange;
